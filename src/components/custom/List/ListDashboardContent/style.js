import {app} from '@src/helpers/constants';

const style = {
  sectionContainer: {flex: 1, padding: 5, margin: 10},
  sectionTitle: {
    fontSize: 30,
    color: app.primaryColor,
    fontFamily: app.primaryFontBold,
    padding: 20,
  },
};

export default style;
